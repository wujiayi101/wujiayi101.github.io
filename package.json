{
  "name": "wu101",
  "type": "module",
  "version": "1.0.0",
  "description": "Personal site / blog. Minimal static generator, no framework.",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node serve.mjs",
    "dev": "node build.mjs && node serve.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
